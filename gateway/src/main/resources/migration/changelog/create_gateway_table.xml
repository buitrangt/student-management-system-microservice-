<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- Tạo bảng scope -->
    <changeSet id="create-scope" author="trang">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="scope"/>
            </not>
        </preConditions>
        <createTable tableName="scope">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="pattern" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="serviceId" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="isdelete" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="method" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- Tạo bảng role_scope -->
    <changeSet id="create-role-scope" author="trang">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="role_scope"/>
            </not>
        </preConditions>
        <createTable tableName="role_scope">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="userId" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="serviceId" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="role" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="scopeId" type="BIGINT">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- Thêm khóa ngoại từ bảng role_scope đến scope -->
    <changeSet id="add-foreign-key" author="trang">
        <addForeignKeyConstraint
                baseTableName="role_scope"
                baseColumnNames="scopeId"
                referencedTableName="scope"
                referencedColumnNames="id"
                constraintName="fk_role_scope_scopeId"/>
    </changeSet>

</databaseChangeLog>
